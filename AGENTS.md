This is a Wails based Go app. 

Warn about using any package that is not a pure Go package and will not build with the standard cross-build (no CGO).
Prefer pure Go packages and buildign with standard cross-build.


